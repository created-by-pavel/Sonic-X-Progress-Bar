package ru.crbpavel.progressbar;

import javax.swing.*;

public interface Icons {
    ImageIcon MARIO = new ImageIcon(Icons.class.getResource("/sonic.gif"));
    ImageIcon SHELL = new ImageIcon(Icons.class.getResource("/chanred.gif"));
}
