package ru.crbpavel.progressbar;

import javax.swing.*;

public interface Icons {
    ImageIcon SONIC = new ImageIcon(Icons.class.getResource("/sonic.gif"));
    ImageIcon CHANRED = new ImageIcon(Icons.class.getResource("/chanred.gif"));
}
